apiVersion: v1
kind: Service
metadata:
  name: chesssvc
  namespace: resume
  labels:
    app: chess_website
spec:
  selector:
    app: chess_website
    tier: deploy
  ports:
  - port: 80
    targetPort: 8000
  type: LoadBalancer
